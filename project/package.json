{
  "name": "one-commerce-project",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "dependencies": {
    "next": "14.2.35"
  },
  "scripts": {
    "build": "npm run build --workspace=frontend",
    "start": "npm run start --workspace=frontend",
    "dev": "npm run dev --workspace=frontend"
  }
}
